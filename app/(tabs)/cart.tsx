import { ScrollView, Text } from "react-native";

export default function Cart(){
    (<ScrollView className="flex-1 mt-5 bg-white">
        <Text className="text-white font-bold mb-8 text-2xl">
            Carrito
        </Text>
    </ScrollView>)
}
